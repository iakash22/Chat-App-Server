const authContollers = require('./auth');
const profileControllers = require('./profile');
const requestControllers = require('./request');

module.exports = {
    authContollers: authContollers,
    profileControllers: profileControllers,
    requestControllers : requestControllers,
};
